	unsigned char *pData = (unsigned char*) imagebuffer->m_pData;
	const int iDataSize = imagebuffer->m_iBufferSize;

	double* numbers = (double*) pData;
	for (int i = 0; i < 10; i++)
		printf("%d ", (int)numbers[i]);

