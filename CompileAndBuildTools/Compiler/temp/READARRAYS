	FILE* fA = fopen(*file, "r");
	if (fA == NULL)
	{
		printf("Input files doesn't exists. Exiting\n");
		exit(3);
	}

	for (int i = 0; i < n; i++)
	{
		int nrA;
		fscanf(fA, "%d", &nrA);	
	SetInputItemToVector(33, &numbers, i, "number", nrA);
	}
